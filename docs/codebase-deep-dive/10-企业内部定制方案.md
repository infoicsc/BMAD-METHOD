# 10 - 企业内部定制方案

> **阅读时间**: 30-45 分钟
> **难度级别**: ⭐⭐⭐⭐☆ 企业实战

## 📋 本章目标

- ✅ 了解如何将 BMAD 改造为企业内部专用框架
- ✅ 掌握移除不需要组件的方法
- ✅ 学会集成企业工具链
- ✅ 理解部署和维护策略

---

## 1. 精简策略

### 场景：只保留核心功能

**目标**: 移除 95% 的功能，只保留基础工作流引擎

**步骤**:

```bash
# 1. Fork 项目
git clone https://github.com/yourusername/BMAD-METHOD.git
cd BMAD-METHOD

# 2. 删除不需要的模块
rm -rf src/modules/bmm
rm -rf src/modules/cis
rm -rf src/modules/bmgd
# 保留 bmb 用于定制工具

# 3. 精简核心
# 只保留必要的 tasks
cd src/core/tasks
# 保留 workflow.xml
# 删除其他可选任务

# 4. 重新构建
npm run build

# 5. 测试安装
cd test-project
npm link ../BMAD-METHOD
npx bmad-method install
```

**结果**:
- 体积减少 80%
- 安装时间减少 90%
- 只保留工作流引擎核心

---

## 2. 企业流程集成

### 场景：集成 JIRA + Confluence

**创建企业模块**: `src/modules/enterprise/`

```yaml
# workflows/jira-story/workflow.yaml
name: "jira-story"
description: "创建 JIRA Story 并同步到 Confluence"

config_source: "{project-root}/bmad/enterprise/config.yaml"
jira_url: "{config_source}:jira_url"
jira_token: "{config_source}:jira_token"
confluence_url: "{config_source}:confluence_url"

instructions: "{installed_path}/instructions.md"
```

```xml
<!-- instructions.md -->
<invoke-task task="{project-root}/bmad/core/tasks/workflow.xml"/>

## Step 1: 收集Story信息

<ask>Story标题？</ask>
<ask>Story描述？</ask>
<ask>验收标准？</ask>

## Step 2: 创建JIRA Story

<action>
使用 JIRA API 创建 Story:
- POST {jira_url}/rest/api/2/issue
- 设置 project, summary, description
- 添加 acceptance criteria
</action>

## Step 3: 同步到Confluence

<action>
在 Confluence 创建页面:
- 链接到 JIRA Story
- 包含详细设计
- 添加到项目空间
</action>

## Step 4: 通知团队

<action>
发送通知:
- Slack webhook
- Email to stakeholders
</action>
```

---

## 3. 品牌定制

### 重命名和重新品牌化

```javascript
// 修改 package.json
{
  "name": "@yourcompany/dev-assistant",
  "description": "YourCompany Internal Development Assistant",
  "version": "1.0.0"
}

// 修改 CLI 显示
// tools/cli/bmad-cli.js
const figlet = require('figlet');
console.log(figlet.textSync('YourCompany DevAssist'));

// 修改 Agent 名称
// src/core/agents/bmad-master.agent.yaml
metadata:
  name: "DevAssist Master"
  title: "YourCompany Development Assistant"
  icon: "🏢"
```

---

## 4. 安全和合规

### 添加审计日志

```yaml
# src/core/workflows/audit-wrapper/workflow.yaml
name: "audit-wrapper"
description: "包装所有工作流，添加审计日志"

instructions: |
  ## Pre-execution

  <action>
  记录审计日志:
  - 用户: {user_name}
  - 时间: {date}
  - 工作流: {workflow_name}
  - 输入参数: {params}
  </action>

  ## Execute wrapped workflow

  <invoke-workflow path="{target_workflow}"/>

  ## Post-execution

  <action>
  记录完成日志:
  - 状态: {status}
  - 输出: {output_files}
  - 持续时间: {duration}
  - 发送到合规系统
  </action>
```

### 代码扫描集成

```yaml
# workflows/secure-dev/workflow.yaml
critical_actions:
  - "运行 SonarQube 扫描"
  - "运行 SAST 工具"
  - "检查依赖漏洞 (npm audit)"
  - "验证许可证合规性"
```

---

## 5. 部署方案

### 内部 NPM Registry

```bash
# 1. 设置 Verdaccio (私有 npm registry)
npm install -g verdaccio
verdaccio

# 2. 发布到内部 registry
npm config set registry http://your-npm-registry:4873
npm publish

# 3. 团队使用
npm install @yourcompany/dev-assistant
```

### Docker 容器化

```dockerfile
# Dockerfile
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --production

COPY . .

ENTRYPOINT ["npm", "run", "bmad"]
```

---

## 6. 真实案例

### 案例 1: 金融公司精简版

**需求**:
- 只需要技术文档生成
- 严格的安全审查
- 集成内部工具

**方案**:
```
保留模块:
- Core (基础)
- 自定义 FinTech 模块

新增工作流:
- security-review (安全审查)
- compliance-check (合规检查)
- audit-trail (审计追踪)

集成:
- ServiceNow (工单系统)
- Splunk (日志分析)
- Checkmarx (代码扫描)
```

### 案例 2: 创业公司快速版

**需求**:
- 极简流程
- 快速迭代
- 最小化配置

**方案**:
```
只保留:
- BMad Master
- Developer Agent
- Tech Spec Workflow

所有选项默认值:
- 跳过可选步骤
- #yolo 模式
- 最少提问
```

---

## 7. 维护策略

### 更新管理

```bash
# 1. 设置上游
git remote add upstream https://github.com/original/BMAD-METHOD.git

# 2. 定期同步核心更新
git fetch upstream
git merge upstream/main --no-commit

# 3. 解决冲突
# 优先保留企业定制

# 4. 测试后发布
npm run test
npm run build
npm version patch
npm publish
```

### 团队培训

**培训材料**:
- 内部文档 Wiki
- 视频教程
- 实战 Workshop
- FAQ 和最佳实践

---

## 8. 成本效益分析

### 投入

| 项目 | 时间 | 成本 |
|------|------|------|
| 初始定制 | 2-4 周 | 中 |
| 集成企业工具 | 1-2 周 | 中 |
| 培训团队 | 1 周 | 低 |
| 持续维护 | 1-2 天/月 | 低 |

### 收益

- ✅ 标准化开发流程
- ✅ 提升团队效率 30-50%
- ✅ 减少文档遗漏 90%
- ✅ 改善代码质量
- ✅ 新人上手时间减少 70%

---

## 9. 快速检查清单

### 定制前

- [ ] 明确企业需求
- [ ] 评估现有流程
- [ ] 识别必需功能
- [ ] 规划集成点

### 定制中

- [ ] Fork 并重命名
- [ ] 移除不需要的模块
- [ ] 添加企业工作流
- [ ] 集成企业工具
- [ ] 更新品牌元素

### 定制后

- [ ] 完整测试
- [ ] 编写内部文档
- [ ] 培训团队
- [ ] 部署到生产
- [ ] 收集反馈

---

## 10. 获取支持

- 📖 参考官方文档
- 💬 社区讨论
- 🐛 提交 Issue
- 🤝 贡献改进

---

**总结**:

BMAD-METHOD 的模块化设计使其非常适合企业定制。通过精简、集成和品牌化，你可以打造完全符合企业需求的内部开发助手。

关键是：
- ✅ 保持核心框架不变
- ✅ 通过模块化扩展
- ✅ 使用 Update-Safe 定制
- ✅ 持续维护和改进

---

➡️ **下一步**: 开始你的企业定制之旅！
