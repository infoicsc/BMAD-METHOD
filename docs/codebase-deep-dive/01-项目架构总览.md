# 01 - 项目架构总览

> **阅读时间**: 30-45 分钟
> **难度级别**: ⭐⭐☆☆☆ 入门
> **前置知识**: 基本的软件架构概念

## 📋 本章目标

阅读完本章后，你将能够：

- ✅ 理解 BMAD-METHOD 的整体设计哲学
- ✅ 掌握项目的层次架构和模块组织
- ✅ 了解技术栈选型的原因
- ✅ 熟悉目录结构和文件组织规范
- ✅ 理解核心概念和术语

---

## 1. 设计哲学与核心理念

### 1.1 Human-in-the-Loop AI 开发

BMAD-METHOD 的核心理念是 **人类放大（Human Amplification）** 而非完全自动化。

```
传统自动化开发                     BMAD-METHOD
┌─────────────────┐               ┌─────────────────┐
│   输入需求       │               │   输入需求       │
└────────┬────────┘               └────────┬────────┘
         │                                 │
         ▼                                 ▼
┌─────────────────┐               ┌─────────────────┐
│   AI 生成代码   │               │  AI 辅助规划    │
│   (黑盒过程)    │               │  (透明过程)     │
└────────┬────────┘               └────────┬────────┘
         │                                 │
         ▼                                 ▼
┌─────────────────┐               ┌─────────────────┐
│   直接输出代码   │               │  人类审核决策   │
└─────────────────┘               └────────┬────────┘
                                           │
                                           ▼
                                  ┌─────────────────┐
                                  │  AI 辅助实现    │
                                  └────────┬────────┘
                                           │
                                           ▼
                                  ┌─────────────────┐
                                  │  迭代优化输出   │
                                  └─────────────────┘
```

**核心差异**:
- ❌ 不追求一键生成完整应用
- ✅ 强调结构化的思考和决策过程
- ✅ AI 扮演专业顾问角色，人类保留最终决策权
- ✅ 输出高质量的规划文档，而非仅仅代码

### 1.2 多 Agent 协作系统

BMAD 模拟真实的软件开发团队，每个 Agent 代表一个专业角色：

```
                    ┌──────────────────┐
                    │   BMad Master    │
                    │   (协调器)       │
                    └────────┬─────────┘
                             │
        ┌────────────────────┼────────────────────┐
        │                    │                    │
   ┌────▼────┐         ┌────▼────┐         ┌────▼────┐
   │   PM    │         │ Architect│        │Developer│
   │ (产品)  │◄───────►│ (架构)   │◄──────►│ (开发)  │
   └────┬────┘         └────┬────┘         └────┬────┘
        │                   │                    │
        │              ┌────▼────┐               │
        │              │   UX    │               │
        │              │ (设计)  │               │
        │              └────┬────┘               │
        │                   │                    │
        └───────────────────┼────────────────────┘
                            │
                       ┌────▼────┐
                       │   TEA   │
                       │ (测试)  │
                       └─────────┘
```

每个 Agent:
- 有独特的 **Persona**（专业背景、沟通风格）
- 提供专门的 **Workflows**（工作流程）
- 基于最佳实践的 **Prompts**（指令模板）

### 1.3 规模自适应架构（3-Track System）

项目支持 3 种复杂度轨道，自动适配项目规模：

```
Project Complexity
    │
    │  Enterprise Track (2-4 weeks)
    │  ├─ 完整的 BMad Method
    │  ├─ + Security Strategy
    │  ├─ + DevOps Planning
    │  └─ + Extended Test Architecture
    │
    │  BMad Method Track (1-2 weeks)
    │  ├─ Analysis (可选)
    │  ├─ Planning (PRD + Stories + UX)
    │  ├─ Solutioning (Architecture)
    │  └─ Implementation (Story-centric)
    │
    │  Quick Flow Track (2-3 hours)
    │  ├─ Tech Spec only
    │  └─ Direct Implementation
    │
    └─────────────────────────────────► Time

Small Bug Fix          Feature          Enterprise System
```

### 1.4 Just-in-Time Context（即时上下文）

为了优化 token 使用，BMAD 采用即时上下文策略：

```
传统方式:                         BMAD 方式:
┌───────────────────┐            ┌───────────────────┐
│ 加载所有文档      │            │ 仅加载当前需要的  │
│ (浪费 tokens)     │            │ (精确加载)        │
└─────────┬─────────┘            └─────────┬─────────┘
          │                                │
          ▼                                ▼
    ┌─────────┐                      ┌─────────┐
    │ Step 1  │                      │ Step 1  │
    │ (90%    │                      │ (100%   │
    │ unused) │                      │ relevant)│
    └─────────┘                      └─────────┘
          │                                │
          ▼                                ▼
    ┌─────────┐                      ┌─────────┐
    │ Step 2  │                      │ Step 2  │
    │ (90%    │                      │ (100%   │
    │ unused) │                      │ relevant)│
    └─────────┘                      └─────────┘
```

实现机制：
- **智能文件发现**: 自动查找相关输入文件
- **文档分片**: 大文档自动分割，按需加载
- **推荐输入**: 工作流声明依赖，按需提示

---

## 2. 整体架构设计

### 2.1 层次架构

BMAD-METHOD 采用经典的分层架构：

```
┌─────────────────────────────────────────────────────────┐
│                    用户交互层 (User Interface Layer)      │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐     │
│  │ Claude Code │  │   Cursor    │  │  Windsurf   │ ... │
│  │  (.claude)  │  │  (.cursor)  │  │ (.windsurf) │     │
│  └─────────────┘  └─────────────┘  └─────────────┘     │
└────────────────────────────┬────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────┐
│                    CLI 工具层 (CLI Layer)                │
│  ┌──────────────────────────────────────────────────┐   │
│  │  bmad-cli (Commander.js)                         │   │
│  │  ├─ install   ├─ build    ├─ status             │   │
│  │  ├─ uninstall ├─ update   └─ list               │   │
│  └──────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────┐
│                   编译构建层 (Build Layer)                │
│  ┌──────────────────────────────────────────────────┐   │
│  │  YamlXmlBuilder                                  │   │
│  │  ├─ YAML → Markdown (Claude Code)               │   │
│  │  ├─ YAML → XML (Web Bundles)                    │   │
│  │  └─ Template Processing                         │   │
│  └──────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────┐
│                   核心框架层 (Core Framework)             │
│  ┌──────────────────────────────────────────────────┐   │
│  │  BMAD-CORE                                       │   │
│  │  ├─ BMad Master Agent (协调器)                   │   │
│  │  ├─ Task Engine (workflow.xml, adv-elicit.xml)  │   │
│  │  ├─ Module Installer                            │   │
│  │  ├─ Variable Resolver                           │   │
│  │  └─ Config Loader                               │   │
│  └──────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────┘
                             │
┌────────────────────────────┼────────────────────────────┐
│                   业务模块层 (Business Module Layer)      │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐   │
│  │   BMM   │  │   BMB   │  │   CIS   │  │  BMGD   │   │
│  │ (方法论) │  │ (构建器)│  │ (创意)  │  │ (游戏)  │   │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘   │
└─────────────────────────────────────────────────────────┘
         │              │              │              │
         ▼              ▼              ▼              ▼
┌─────────────────────────────────────────────────────────┐
│                   数据和配置层 (Data & Config Layer)      │
│  ┌──────────────────────────────────────────────────┐   │
│  │  Agent YAML  │  Workflow YAML  │  Task XML       │   │
│  │  Config YAML │  Templates MD   │  Data CSV       │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 2.2 核心组件关系

```
┌──────────────────────────────────────────────────────────┐
│                        Project                            │
│  ┌────────────────────────────────────────────────────┐  │
│  │  bmad/ (安装目录)                                   │  │
│  │  ├─ _cfg/                                          │  │
│  │  │  ├─ manifest.yaml         # 安装元数据         │  │
│  │  │  ├─ agent-manifest.csv    # Agent 清单         │  │
│  │  │  └─ workflow-manifest.csv # Workflow 清单      │  │
│  │  │                                                 │  │
│  │  ├─ core/                    # 核心框架            │  │
│  │  │  ├─ agents/                                     │  │
│  │  │  │  └─ bmad-master.md                          │  │
│  │  │  ├─ tasks/                                      │  │
│  │  │  │  ├─ workflow.xml                            │  │
│  │  │  │  ├─ adv-elicit.xml                          │  │
│  │  │  │  └─ validate-workflow.xml                   │  │
│  │  │  └─ workflows/                                  │  │
│  │  │     ├─ party-mode/                              │  │
│  │  │     └─ brainstorming/                           │  │
│  │  │                                                 │  │
│  │  └─ bmm/                     # BMM 模块            │  │
│  │     ├─ agents/                                     │  │
│  │     │  ├─ pm.agent.yaml                           │  │
│  │     │  ├─ architect.agent.yaml                    │  │
│  │     │  └─ ...                                     │  │
│  │     ├─ workflows/                                  │  │
│  │     │  ├─ 1-analysis/                             │  │
│  │     │  ├─ 2-plan-workflows/                       │  │
│  │     │  ├─ 3-solutioning/                          │  │
│  │     │  └─ 4-implementation/                       │  │
│  │     └─ config.yaml           # BMM 配置            │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │  .claude/ (Claude Code 集成)                       │  │
│  │  ├─ commands/                                      │  │
│  │  │  ├─ bmad-core-agents-bmad-master.md            │  │
│  │  │  ├─ bmad-bmm-agents-pm.md                      │  │
│  │  │  └─ bmad-bmm-workflows-prd.md                  │  │
│  │  └─ .slash-command-aliases                        │  │
│  └────────────────────────────────────────────────────┘  │
│                                                           │
│  ┌────────────────────────────────────────────────────┐  │
│  │  project-output/ (工作流输出)                       │  │
│  │  ├─ PRD.md                                         │  │
│  │  ├─ architecture.md                                │  │
│  │  ├─ tech-spec.md                                   │  │
│  │  ├─ bmm-workflow-status.yaml                      │  │
│  │  └─ ...                                            │  │
│  └────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────┘
```

### 2.3 数据流概览

```
1. 安装阶段
   ┌──────────┐     ┌──────────┐     ┌──────────┐
   │  Source  │────►│ Compiler │────►│ Installed│
   │   YAML   │     │  Builder │     │  Format  │
   └──────────┘     └──────────┘     └──────────┘
   src/modules/      YamlXmlBuilder   bmad/ + .claude/
   *.agent.yaml      *.workflow.yaml  *.md

2. 执行阶段
   ┌──────────┐     ┌──────────┐     ┌──────────┐
   │   User   │────►│  Agent   │────►│ Workflow │
   │  Trigger │     │  Menu    │     │  Execute │
   └──────────┘     └──────────┘     └──────────┘
   *create-prd       pm.agent.yaml    workflow.yaml
                                      instructions.md
        │                                   │
        └───────────────┬───────────────────┘
                        ▼
                ┌──────────────┐
                │ Task Engine  │
                │ workflow.xml │
                └──────┬───────┘
                       │
                       ▼
                ┌──────────────┐
                │   Output     │
                │  PRD.md      │
                └──────────────┘

3. 配置解析
   ┌──────────────┐
   │config.yaml   │
   │project_name: │
   │  "MyApp"     │
   └──────┬───────┘
          │
          ▼
   ┌──────────────────────────────┐
   │ workflow.yaml                │
   │ config_source:               │
   │   "{project-root}/...yaml"   │
   │ project_name:                │
   │   "{config_source}:project_  │
   │    name"                     │
   └──────┬───────────────────────┘
          │ Variable Resolver
          ▼
   ┌──────────────┐
   │ "MyApp"      │
   │ (resolved)   │
   └──────────────┘
```

---

## 3. 技术栈详解

### 3.1 为什么选择 Node.js？

**原因分析**:

1. **跨平台性**
   - Windows、macOS、Linux 统一体验
   - npm 生态系统成熟

2. **轻量级**
   - 不需要编译，即装即用
   - 适合 CLI 工具开发

3. **丰富的生态**
   - Commander.js (CLI 框架)
   - Inquirer.js (交互式提示)
   - js-yaml (YAML 解析)
   - xml2js (XML 处理)

4. **IDE 集成友好**
   - 大多数 IDE 插件基于 Node.js
   - 易于集成到现有工作流

**关键依赖**:

```json
{
  "commander": "^14.0.0",       // CLI 命令框架
  "inquirer": "^8.2.6",         // 交互式提示
  "js-yaml": "^4.1.0",          // YAML 解析
  "xml2js": "^0.6.2",           // XML 解析
  "chalk": "^4.1.2",            // 终端颜色
  "ora": "^5.4.1",              // 加载动画
  "boxen": "^5.1.2",            // 终端框
  "glob": "^11.0.3",            // 文件匹配
  "fs-extra": "^11.3.0",        // 增强文件系统
  "semver": "^7.6.3"            // 版本管理
}
```

### 3.2 为什么选择 YAML？

**Agent 和 Workflow 定义使用 YAML**:

```yaml
# ✅ 优点：人类可读性强
agent:
  metadata:
    name: John
    title: Product Manager

  persona:
    role: Strategic PM
    principles:
      - User-first mindset
      - Data-driven decisions
```

对比 JSON:

```json
// ❌ 缺点：语法冗余
{
  "agent": {
    "metadata": {
      "name": "John",
      "title": "Product Manager"
    },
    "persona": {
      "role": "Strategic PM",
      "principles": [
        "User-first mindset",
        "Data-driven decisions"
      ]
    }
  }
}
```

**YAML 的优势**:
- ✅ 支持注释（对配置说明很重要）
- ✅ 无需大量引号和括号
- ✅ 支持多行字符串（适合 prompts）
- ✅ 层次清晰（缩进表示）

### 3.3 为什么选择 XML（仅用于 Task）？

**Task 定义使用 XML**:

```xml
<workflow-rules>
  <rule n="1">Execute steps in numerical order</rule>
  <rule n="2">Optional steps: ask user unless #yolo</rule>
</workflow-rules>

<flow>
  <step n="1" title="Load Configuration">
    <substep n="1a">
      <mandate>Load config_source</mandate>
    </substep>
  </step>
</flow>
```

**XML 的优势**（对于复杂流程控制）:
- ✅ 强类型结构（属性 + 内容）
- ✅ 适合表达嵌套流程（step > substep > phase）
- ✅ 支持元数据（属性如 `n`, `title`, `critical`）
- ✅ LLM 解析能力强

**为什么不用 YAML？**
- ❌ 复杂嵌套时可读性下降
- ❌ 难以区分属性和内容
- ❌ 数字顺序（1, 2, 3）在 YAML 中不自然

### 3.4 为什么选择 Markdown？

**指令、模板、输出使用 Markdown**:

```markdown
# PRD Template

## Product Overview
{project_name} is a ...

## User Stories
1. As a user, I want to...
2. As an admin, I need to...
```

**Markdown 的优势**:
- ✅ 开发者熟悉
- ✅ Git 友好（易于 diff 和 merge）
- ✅ 可直接在 IDE 中预览
- ✅ 支持代码块、表格、列表
- ✅ LLM 生成和理解都很好

### 3.5 技术栈组合的协同效果

```
┌─────────────────────────────────────────────────┐
│ 配置和定义 (YAML)                                │
│ - Agent 定义（persona, menu）                   │
│ - Workflow 配置（inputs, outputs, variables）   │
│ - 模块配置（install-config.yaml）               │
│                                                 │
│ 优势：易编辑、易维护、支持注释                   │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│ 流程控制 (XML)                                   │
│ - Task 定义（workflow.xml, adv-elicit.xml）     │
│ - 复杂流程逻辑（step, substep, phase）          │
│                                                 │
│ 优势：强结构、适合复杂嵌套、LLM 友好             │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│ 内容和模板 (Markdown)                            │
│ - 工作流指令（instructions.md）                  │
│ - 输出模板（prd-template.md）                   │
│ - 用户文档（README.md）                          │
│                                                 │
│ 优势：可读性强、Git 友好、开发者熟悉             │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│ 数据表格 (CSV)                                   │
│ - 项目类型检测（project-types.csv）             │
│ - 引导方法库（adv-elicit-methods.csv）          │
│                                                 │
│ 优势：结构化数据、易于编辑、可用电子表格工具      │
└───────────────────┬─────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────┐
│ 运行时处理 (JavaScript/Node.js)                  │
│ - 编译和构建（YamlXmlBuilder）                   │
│ - 安装和部署（installer.js）                    │
│ - CLI 命令（bmad-cli.js）                       │
│                                                 │
│ 优势：跨平台、生态丰富、集成容易                 │
└─────────────────────────────────────────────────┘
```

---

## 4. 目录结构深度解析

### 4.1 源代码目录（src/）

```
src/                                    # 源代码根目录 (3.6MB)
│
├── core/                               # 核心框架 (122KB)
│   ├── agents/                         # 核心 Agent
│   │   └── bmad-master.agent.yaml      # 主协调器 Agent
│   │
│   ├── workflows/                      # 核心工作流
│   │   ├── party-mode/                 # 多 Agent 协作模式
│   │   │   ├── workflow.yaml
│   │   │   └── instructions.md
│   │   └── brainstorming/              # 头脑风暴工作流
│   │       ├── workflow.yaml
│   │       └── instructions.md
│   │
│   ├── tasks/                          # 核心任务定义
│   │   ├── workflow.xml                # 通用工作流引擎
│   │   ├── adv-elicit.xml              # 高级信息收集
│   │   ├── validate-workflow.xml       # 工作流验证
│   │   ├── index-docs.xml              # 文档索引
│   │   └── adv-elicit-methods.csv      # 36种引导方法
│   │
│   ├── _module-installer/              # 安装器
│   │   ├── install-config.yaml         # 安装配置
│   │   ├── installer.js                # 安装逻辑
│   │   └── ide-adapters/               # IDE 适配器
│   │       ├── base-ide.js             # 基类
│   │       ├── claude-code.js          # Claude Code
│   │       ├── cursor.js               # Cursor
│   │       └── ... (15个 IDE)
│   │
│   └── README.md                       # 核心文档
│
├── modules/                            # 业务模块 (3.5MB)
│   │
│   ├── bmm/                            # BMad Method (2.3MB)
│   │   ├── agents/                     # 8个 Agent
│   │   │   ├── pm.agent.yaml           # 产品经理
│   │   │   ├── analyst.agent.yaml      # 分析师
│   │   │   ├── architect.agent.yaml    # 架构师
│   │   │   ├── scrum-master.agent.yaml # Scrum Master
│   │   │   ├── developer.agent.yaml    # 开发者
│   │   │   ├── tea.agent.yaml          # 测试架构师
│   │   │   ├── ux-designer.agent.yaml  # UX 设计师
│   │   │   └── tech-writer.agent.yaml  # 技术文档
│   │   │
│   │   ├── workflows/                  # 30+ 工作流
│   │   │   ├── 1-analysis/             # 阶段1：分析
│   │   │   │   ├── brainstorm-project/
│   │   │   │   ├── product-brief/
│   │   │   │   └── market-research/
│   │   │   │
│   │   │   ├── 2-plan-workflows/       # 阶段2：规划
│   │   │   │   ├── prd/                # PRD 工作流
│   │   │   │   │   ├── workflow.yaml
│   │   │   │   │   ├── instructions.md
│   │   │   │   │   ├── prd-template.md
│   │   │   │   │   ├── checklist.md
│   │   │   │   │   └── project-types.csv
│   │   │   │   ├── tech-spec/          # 技术规格
│   │   │   │   └── create-epics-and-stories/
│   │   │   │
│   │   │   ├── 3-solutioning/          # 阶段3：方案
│   │   │   │   ├── architecture/
│   │   │   │   ├── ux-design/
│   │   │   │   └── ...
│   │   │   │
│   │   │   ├── 4-implementation/       # 阶段4：实现
│   │   │   │   ├── dev-story/
│   │   │   │   └── ...
│   │   │   │
│   │   │   ├── workflow-status/        # 工作流状态管理
│   │   │   ├── document-project/       # 棕地项目文档化
│   │   │   ├── testarch/               # 测试架构
│   │   │   └── techdoc/                # 技术文档
│   │   │
│   │   ├── _module-installer/
│   │   │   └── install-config.yaml     # BMM 安装配置
│   │   │
│   │   └── README.md
│   │
│   ├── bmb/                            # BMad Builder (463KB)
│   │   ├── agents/
│   │   │   └── bmad-builder.agent.yaml
│   │   │
│   │   ├── workflows/                  # 7个工作流
│   │   │   ├── create-agent/
│   │   │   ├── create-workflow/
│   │   │   ├── create-module/
│   │   │   ├── edit-agent/
│   │   │   ├── edit-workflow/
│   │   │   ├── audit-workflow/
│   │   │   └── redoc/
│   │   │
│   │   └── _module-installer/
│   │
│   ├── cis/                            # Creative Intelligence (154KB)
│   │   ├── agents/                     # 5个创意 Agent
│   │   ├── workflows/                  # 5个创意工作流
│   │   └── _module-installer/
│   │
│   └── bmgd/                           # Game Development (556KB)
│       ├── agents/                     # 4个游戏开发 Agent
│       ├── workflows/                  # 游戏开发工作流
│       └── _module-installer/
│
└── utility/                            # 工具函数 (35KB)
    ├── config-loader.js                # 配置加载
    ├── variable-resolver.js            # 变量解析
    └── file-utils.js                   # 文件工具
```

### 4.2 工具目录（tools/）

```
tools/                                  # 开发工具 (741KB)
│
├── cli/                                # CLI 工具 (627KB)
│   ├── bmad-cli.js                     # CLI 入口
│   │
│   ├── commands/                       # CLI 命令
│   │   ├── install.js                  # 安装命令
│   │   ├── build.js                    # 构建命令
│   │   ├── status.js                   # 状态查看
│   │   ├── list.js                     # 列出项目
│   │   ├── uninstall.js                # 卸载命令
│   │   └── update.js                   # 更新命令
│   │
│   ├── lib/                            # 核心库
│   │   ├── yaml-xml-builder.js         # YAML→MD/XML 编译器
│   │   ├── installer.js                # 安装逻辑
│   │   ├── ui.js                       # 交互式UI
│   │   └── utils.js                    # 工具函数
│   │
│   ├── bundlers/                       # 打包器
│   │   └── web-bundler.js              # Web Bundle 生成
│   │
│   └── README.md
│
├── flattener/                          # 文档平铺工具 (79KB)
│   ├── flatten.js                      # 多文件→单文件
│   └── unflatten.js                    # 单文件→多文件
│
└── schema/                             # Schema 验证
    ├── agent.js                        # Agent YAML Schema (Zod)
    ├── workflow.js                     # Workflow Schema
    └── task.js                         # Task Schema
```

### 4.3 编译输出目录（bmad/）

```
bmad/                                   # 安装目录 (622KB)
│
├── _cfg/                               # 配置目录
│   ├── manifest.yaml                   # 安装元数据
│   ├── agent-manifest.csv              # Agent 清单
│   ├── workflow-manifest.csv           # Workflow 清单
│   │
│   └── agents/                         # Agent 定制配置
│       └── pm-custom.yaml              # (用户可选)
│
├── core/                               # 编译后的核心
│   ├── agents/
│   │   └── bmad-master.md              # Markdown 格式
│   │
│   ├── tasks/
│   │   ├── workflow.xml
│   │   ├── adv-elicit.xml
│   │   └── ...
│   │
│   └── workflows/
│       ├── party-mode/
│       └── brainstorming/
│
├── bmm/                                # 编译后的 BMM
│   ├── agents/
│   │   ├── pm.agent.yaml               # 保持 YAML
│   │   ├── architect.agent.yaml
│   │   └── ...
│   │
│   ├── workflows/
│   │   ├── 1-analysis/
│   │   ├── 2-plan-workflows/
│   │   ├── 3-solutioning/
│   │   └── 4-implementation/
│   │
│   └── config.yaml                     # BMM 运行时配置
│
├── bmb/                                # 编译后的 BMB
├── cis/                                # 编译后的 CIS
├── bmgd/                               # 编译后的 BMGD
│
└── docs/                               # 编译后的文档
```

### 4.4 IDE 集成目录（.claude/）

```
.claude/                                # Claude Code 集成 (67KB)
│
├── commands/                           # Slash 命令
│   ├── bmad-core-agents-bmad-master.md
│   ├── bmad-core-workflows-party-mode.md
│   ├── bmad-bmm-agents-pm.md
│   ├── bmad-bmm-agents-architect.md
│   ├── bmad-bmm-workflows-prd.md
│   └── ... (每个 Agent 和 Workflow 一个文件)
│
└── .slash-command-aliases             # 别名配置
    bmad:core:agents:bmad-master
    bmad:bmm:agents:pm
    bmad:bmm:workflows:prd
    ...
```

**其他 IDE 类似**:
- `.cursor/` (Cursor IDE)
- `.windsurf/` (Windsurf IDE)
- `.github/copilot/` (GitHub Copilot)
- 等等...

### 4.5 测试目录（test/）

```
test/                                   # 测试文件 (167KB)
│
├── test-agent-schema.js                # Agent Schema 测试
├── test-installation-components.js     # 安装组件测试
├── test-cli-integration.sh             # CLI 集成测试
│
└── fixtures/                           # 测试数据
    ├── valid-agent.yaml
    ├── invalid-agent.yaml
    └── sample-workflow.yaml
```

### 4.6 文档目录（docs/）

```
docs/                                   # 项目文档 (73KB)
│
├── document-sharding-guide.md          # 文档分片指南
├── v4-to-v6-upgrade.md                 # 升级指南
├── ide-support.md                      # IDE 支持文档
└── best-practices.md                   # 最佳实践
```

---

## 5. 核心概念和术语

### 5.1 Agent（代理）

**定义**: 代表特定专业角色的 AI 助手。

**核心组成**:
```yaml
agent:
  metadata:        # 元数据（ID、名称、图标）
  persona:         # 人设（角色、身份、沟通风格）
  critical_actions:# 关键操作（在加载时执行）
  menu:            # 菜单（可触发的命令）
```

**生命周期**:
```
定义(YAML) → 编译(Markdown/XML) → 安装(IDE) → 加载 → 执行
```

**示例**: PM Agent (产品经理)
- Persona: 战略性产品思维
- Menu: 创建PRD、市场调研、头脑风暴
- Critical Actions: 加载项目配置

### 5.2 Workflow（工作流）

**定义**: 一个结构化的多步骤流程，引导用户完成特定任务。

**核心组成**:
```yaml
workflow:
  config_source:         # 配置来源
  variables:             # 变量定义
  recommended_inputs:    # 推荐输入文件
  instructions:          # 执行指令
  template:              # 输出模板
  checklist:             # 验证清单
```

**执行流程**:
```
触发 → 加载配置 → 解析变量 → 加载输入 → 执行指令 → 生成输出 → 验证
```

**示例**: PRD Workflow
- 输入: Product Brief, Market Research
- 步骤: 分析需求 → 定义用户故事 → 编写规格 → 优先级排序
- 输出: PRD.md

### 5.3 Task（任务）

**定义**: 可重用的流程控制逻辑，定义如何执行工作流。

**核心组成**:
```xml
<task>
  <workflow-rules>   <!-- 执行规则 -->
  <flow>             <!-- 执行流程 -->
    <step>           <!-- 主步骤 -->
      <substep>      <!-- 子步骤 -->
        <phase>      <!-- 执行阶段 -->
```

**示例**: workflow.xml
- 规则: 按顺序执行、可选步骤、用户批准
- 流程: 加载配置 → 处理指令 → 生成输出

### 5.4 Module（模块）

**定义**: 一组相关的 Agent、Workflow、Task 的集合。

**标准结构**:
```
module/
├── agents/              # Agent 定义
├── workflows/           # 工作流定义
├── tasks/               # (可选) 任务定义
├── _module-installer/   # 安装配置
└── README.md            # 模块文档
```

**示例**: BMM (BMad Method)
- 8 个 Agent (PM, Architect, Developer, etc.)
- 30+ Workflows (PRD, Architecture, etc.)
- 4 个开发阶段

### 5.5 Track（轨道）

**定义**: 不同规模项目的执行路径。

**3 种轨道**:
1. **Quick Flow**: 快速修复和小功能
2. **BMad Method**: 标准功能开发
3. **Enterprise**: 企业级系统

**区别**:
```
              Quick     BMad      Enterprise
Analysis      ❌        可选       ✅
PRD           ❌        ✅         ✅
Tech Spec     ✅        ✅         ✅
Architecture  ❌        ✅         ✅
UX Design     ❌        ✅         ✅
Security      ❌        ❌         ✅
DevOps        ❌        ❌         ✅
```

### 5.6 Variable Resolution（变量解析）

**定义**: 动态替换配置中的占位符。

**变量类型**:
```yaml
{project-root}              # 项目根目录
{installed_path}            # 当前工作流路径
{output_folder}             # 输出目录
{config_source}:var_name    # 从配置文件读取
system-generated            # 系统生成（日期等）
```

**解析顺序**:
```
1. 加载配置文件
2. 替换 {config_source}
3. 替换 {config_source}:variable
4. 替换系统变量
5. 替换路径变量
```

### 5.7 Document Sharding（文档分片）

**定义**: 将大文档分割成小片段，按需加载。

**结构**:
```
PRD/                     # 分片目录
├── index.md             # 主索引
├── 01-overview.md       # 第一部分
├── 02-user-stories.md   # 第二部分
└── 03-requirements.md   # 第三部分
```

**优势**:
- ✅ 减少 token 使用 90%+
- ✅ 按需加载相关部分
- ✅ 提高 AI 专注度

### 5.8 Web Bundle（Web 包）

**定义**: 为 Web 版本 Claude 准备的预打包工作流。

**结构**:
```yaml
web_bundle:
  name: "prd"
  description: "创建产品需求文档"
  instructions: "path/to/instructions.md"
  validation: "path/to/checklist.md"
  web_bundle_files:
    - "file1.md"
    - "file2.csv"
```

**生成**: `npm run bundle`

### 5.9 Manifest（清单）

**定义**: 记录安装信息的元数据文件。

**主要清单**:
```
_cfg/manifest.yaml          # 安装版本、模块
_cfg/agent-manifest.csv     # Agent 清单
_cfg/workflow-manifest.csv  # Workflow 清单
```

**用途**:
- 版本管理
- 状态查看 (`bmad status`)
- 更新检测

---

## 6. 依赖关系图谱

### 6.1 模块依赖

```
┌─────────────────────────────────────────────────┐
│                   BMAD-CORE                     │
│  (所有模块的基础)                                │
│  - BMad Master                                  │
│  - workflow.xml                                 │
│  - adv-elicit.xml                               │
└────────────────────┬────────────────────────────┘
                     │
         ┌───────────┼───────────┬────────────┐
         │           │           │            │
    ┌────▼────┐ ┌───▼────┐ ┌───▼────┐  ┌───▼────┐
    │   BMM   │ │  BMB   │ │  CIS   │  │ BMGD   │
    │ (依赖)  │ │ (依赖) │ │ (依赖) │  │ (依赖) │
    └─────────┘ └────────┘ └────────┘  └────────┘
         │           │
         │           └──────────┐
         │                      │
    ┌────▼──────────────────────▼────┐
    │  BMB 可创建自定义 Agent/        │
    │  Workflow，扩展 BMM             │
    └─────────────────────────────────┘
```

**依赖说明**:
- **所有模块** 都依赖 BMAD-CORE
- BMM 可独立使用
- BMB 用于扩展其他模块
- CIS 可与 BMM 结合使用
- BMGD 独立或与 BMM 结合

### 6.2 组件依赖

```
Agent
  │
  ├─ 依赖 → Workflow (通过 menu)
  │
  └─ 依赖 → Config (通过 critical_actions)

Workflow
  │
  ├─ 依赖 → Task (通过 task_file)
  │
  ├─ 依赖 → Config (通过 config_source)
  │
  ├─ 依赖 → Template (通过 template)
  │
  └─ 依赖 → Input Files (通过 recommended_inputs)

Task
  │
  └─ 被调用 by Workflow

Module
  │
  ├─ 包含 → Agents
  │
  ├─ 包含 → Workflows
  │
  ├─ 包含 → Tasks (可选)
  │
  └─ 依赖 → BMAD-CORE
```

### 6.3 编译依赖

```
Source Files (src/)
  │
  ├─ *.agent.yaml ────┐
  │                   │
  ├─ *.workflow.yaml ─┤
  │                   │
  └─ install-config ──┤
                      │
                      ▼
          ┌───────────────────┐
          │ YamlXmlBuilder    │
          │ (编译器)          │
          └─────────┬─────────┘
                    │
        ┌───────────┼───────────┐
        │           │           │
        ▼           ▼           ▼
    Markdown      YAML         XML
    (Claude)    (其他IDE)   (Web Bundle)
        │           │           │
        └───────────┼───────────┘
                    │
                    ▼
            Installed Files (bmad/)
```

---

## 7. 架构特色和亮点

### 7.1 更新安全的定制（Update-Safe Customization）

**问题**: 用户定制后，如何安全更新？

**解决方案**:
```
bmad/
├── bmm/
│   ├── agents/
│   │   └── pm.agent.yaml        # 官方版本
│   │
│   └── config.yaml               # 官方配置
│
└── _cfg/
    ├── agents/
    │   └── pm-custom.yaml        # 用户定制 ✅
    │
    └── bmm-config-custom.yaml    # 用户定制 ✅
```

**加载顺序**:
1. 加载官方文件
2. 检查 `_cfg/` 是否有定制
3. 合并定制（覆盖官方）

**更新时**:
- 更新 `bmm/agents/pm.agent.yaml` (官方)
- 保留 `_cfg/agents/pm-custom.yaml` (用户)
- 用户定制不丢失 ✅

### 7.2 智能文件发现

**问题**: 工作流输入文件可能有多种形式。

**解决方案**:
```yaml
input_file_patterns:
  product_brief:
    whole: "{output_folder}/*brief*.md"
    sharded: "{output_folder}/*brief*/index.md"
```

**查找逻辑**:
1. 先找完整文档 (`*brief*.md`)
2. 未找到则找分片 (`*brief*/index.md`)
3. 找到后自动加载

**用户体验**:
- 用户无需关心文件格式
- 系统自动适配

### 7.3 多语言支持

**问题**: 团队用中文交流，但文档要英文。

**解决方案**:
```yaml
communication_language: "zh-CN"      # AI 用中文交流
document_output_language: "en-US"   # 文档输出英文
```

**效果**:
- 提问和解释用中文
- PRD.md 输出英文
- 最佳用户体验

### 7.4 渐进式采用

**问题**: 团队不想一次性采用所有流程。

**解决方案**: 模块化 + 可选步骤
```
1. 仅安装 BMAD-CORE
   → 使用 BMad Master + 通用任务

2. 添加 BMM
   → 使用部分工作流 (如仅 Tech Spec)

3. 逐步增加工作流
   → PRD → Architecture → Full Process

4. 添加其他模块
   → BMB (创建自定义) → CIS (创意)
```

### 7.5 IDE 无关性

**问题**: 团队用不同 IDE。

**解决方案**: 适配器模式
```
BaseIDE (base-ide.js)
    │
    ├─ ClaudeCodeIDE
    ├─ CursorIDE
    ├─ WindsurfIDE
    └─ ... (15 个 IDE)
```

**每个 IDE**:
- 继承 BaseIDE
- 实现 install(), uninstall()
- 定义文件路径和格式

**用户体验**:
- 一次编写，多 IDE 使用
- 切换 IDE 无缝迁移

---

## 8. 快速参考

### 8.1 关键路径速查

| 需求 | 查看文件 |
|------|---------|
| 理解 Agent 系统 | `src/core/agents/bmad-master.agent.yaml` |
| 理解 Workflow | `src/modules/bmm/workflows/2-plan-workflows/prd/` |
| 理解 Task 系统 | `src/core/tasks/workflow.xml` |
| 理解安装流程 | `src/core/_module-installer/installer.js` |
| 理解编译流程 | `tools/cli/lib/yaml-xml-builder.js` |
| 理解 CLI 命令 | `tools/cli/bmad-cli.js` |
| 创建自定义 Agent | 查看 BMB 模块 `workflows/create-agent/` |
| 创建自定义 Workflow | 查看 BMB 模块 `workflows/create-workflow/` |

### 8.2 命令速查

```bash
# 安装
npx bmad-method@alpha install

# 查看状态
npx bmad-method@alpha status

# 构建（开发）
npm run build

# 生成 Web Bundle
npm run bundle

# 验证
npm run validate:schemas
npm run validate:bundles

# 测试
npm test

# 格式化
npm run format
```

### 8.3 重要概念映射

| BMAD 术语 | 等价概念 | 说明 |
|-----------|---------|------|
| Agent | AI Persona | 特定角色的 AI 助手 |
| Workflow | Process Template | 结构化流程模板 |
| Task | Process Engine | 流程执行引擎 |
| Module | Plugin / Extension | 功能扩展包 |
| Track | Methodology Variant | 方法论变体 |
| Menu | Quick Actions | 快速操作菜单 |
| Trigger | Command Alias | 命令别名 |

---

## 9. 下一步

### 推荐阅读顺序

1. **理解核心**: [02 - 核心系统详解](./02-核心系统详解.md)
2. **深入 Agent**: [03 - 代理系统深度解析](./03-代理系统深度解析.md)
3. **深入 Workflow**: [04 - 工作流系统深度解析](./04-工作流系统深度解析.md)

### 实践建议

在继续阅读前，建议：

1. ✅ 安装 BMAD 到测试项目
2. ✅ 运行一次 PRD 工作流
3. ✅ 查看生成的文件结构
4. ✅ 对比源代码和编译输出

这样你会对架构有更直观的理解。

---

## 10. 总结

BMAD-METHOD 是一个：

- ✅ **人类中心** 的 AI 辅助框架（非自动化工具）
- ✅ **模块化** 的系统（核心 + 可选模块）
- ✅ **多 Agent** 协作平台（模拟真实团队）
- ✅ **规模自适应** 的方法论（3 轨支持）
- ✅ **IDE 无关** 的解决方案（15 个 IDE 支持）
- ✅ **更新安全** 的定制系统（保护用户定制）

通过精心设计的架构，BMAD 在灵活性、可扩展性、易用性之间取得了平衡。

---

➡️ **下一章**: [02 - 核心系统详解](./02-核心系统详解.md)
