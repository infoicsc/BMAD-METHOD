# 03 - 代理系统深度解析

> **阅读时间**: 75-120 分钟
> **难度级别**: ⭐⭐⭐⭐☆ 高级
> **前置知识**: 理解项目架构，熟悉 YAML 格式

## 📋 本章目标

阅读完本章后，你将能够：

- ✅ 完全理解 Agent 的概念模型和生命周期
- ✅ 掌握 Agent YAML Schema 的所有细节
- ✅ 了解 Agent 的编译和部署流程
- ✅ 创建自己的自定义 Agent
- ✅ 修改现有 Agent 的行为和配置
- ✅ 理解 Multi-Agent 协作机制

---

## 1. Agent 概念模型

### 1.1 什么是 Agent？

在 BMAD-METHOD 中，**Agent 是一个具有特定专业角色的 AI 助手**。

**类比**:
```
现实软件团队                BMAD Agent 系统
┌────────────────┐         ┌────────────────┐
│  产品经理 John  │   →    │   PM Agent     │
│  - 负责 PRD     │         │   - 创建 PRD   │
│  - 市场调研     │         │   - 市场分析   │
│  - 需求分析     │         │   - 需求管理   │
└────────────────┘         └────────────────┘

┌────────────────┐         ┌────────────────┐
│  架构师 Alex    │   →    │ Architect Agent│
│  - 系统设计     │         │   - 架构设计   │
│  - 技术选型     │         │   - 技术评估   │
│  - 性能优化     │         │   - 安全审查   │
└────────────────┘         └────────────────┘

┌────────────────┐         ┌────────────────┐
│  开发者 Casey   │   →    │Developer Agent │
│  - 编写代码     │         │   - 代码实现   │
│  - Code Review  │         │   - 重构建议   │
│  - 调试问题     │         │   - 测试支持   │
└────────────────┘         └────────────────┘
```

### 1.2 Agent 的核心特征

每个 Agent 具有以下特征：

#### 1. **Persona（人设）**
```yaml
persona:
  role: "Investigative Product Strategist + Market-Savvy PM"
  identity: "Product management veteran with 8+ years experience..."
  communication_style: "Direct and analytical with stakeholders..."
  principles:
    - "I operate with an investigative mindset..."
    - "My decision-making blends data-driven insights..."
```

**作用**:
- 定义 Agent 的专业背景
- 塑造交流风格
- 设定工作原则

#### 2. **Menu（菜单）**
```yaml
menu:
  - trigger: "create-prd"
    workflow: "{project-root}/bmad/bmm/workflows/.../prd/workflow.yaml"
    description: "Create Product Requirements Document"
```

**作用**:
- 提供可触发的命令
- 链接到具体工作流
- 快速访问常用操作

#### 3. **Critical Actions（关键操作）**
```yaml
critical_actions:
  - "Load into memory {project-root}/bmad/bmm/config.yaml"
  - "Set variable project_name, output_folder"
  - "ALWAYS communicate in {communication_language}"
```

**作用**:
- Agent 加载时自动执行
- 加载配置和上下文
- 设置运行时环境

### 1.3 Agent 生命周期

```
┌─────────────────────────────────────────────────────────┐
│ 1. 定义阶段 (Development)                                │
│    ┌──────────────────────────────────────┐             │
│    │  src/modules/bmm/agents/pm.agent.yaml│             │
│    │  - 编写 Persona                       │             │
│    │  - 定义 Menu Items                    │             │
│    │  - 配置 Critical Actions              │             │
│    └──────────────────────────────────────┘             │
└───────────────────────────┬─────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│ 2. 编译阶段 (Build)                                      │
│    ┌──────────────────────────────────────┐             │
│    │  YamlXmlBuilder                      │             │
│    │  - 验证 YAML Schema                  │             │
│    │  - 解析 Variables                    │             │
│    │  - 生成 Markdown (Claude Code)       │             │
│    │  - 生成 XML (Web Bundle)             │             │
│    └──────────────────────────────────────┘             │
└───────────────────────────┬─────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│ 3. 安装阶段 (Installation)                               │
│    ┌──────────────────────────────────────┐             │
│    │  Installer                            │             │
│    │  - 复制到 bmad/bmm/agents/            │             │
│    │  - 生成 IDE 命令 (.claude/commands/)  │             │
│    │  - 注册到 manifest.csv                │             │
│    └──────────────────────────────────────┘             │
└───────────────────────────┬─────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────┐
│ 4. 运行阶段 (Runtime)                                    │
│    ┌──────────────────────────────────────┐             │
│    │  User 触发 Agent                      │             │
│    │  - 加载 Agent 配置                    │             │
│    │  - 执行 Critical Actions              │             │
│    │  - 显示 Menu                          │             │
│    │  - 等待用户选择                       │             │
│    │  - 执行 Workflow/Action               │             │
│    └──────────────────────────────────────┘             │
└─────────────────────────────────────────────────────────┘
```

---

## 2. Agent YAML Schema 完整规范

### 2.1 顶层结构

```yaml
agent:                    # 根对象 (必需)
  metadata:               # 元数据 (必需)
  persona:                # 人设 (必需)
  critical_actions:       # 关键操作 (可选)
  menu:                   # 菜单 (必需，至少1项)
  prompts:                # 自定义提示词 (可选)
```

### 2.2 Metadata（元数据）

**完整定义**:
```yaml
metadata:
  id: "bmad/bmm/agents/pm.md"          # Agent ID (必需)
  name: "John"                         # Agent 名称 (必需)
  title: "Product Manager"             # 职位标题 (必需)
  icon: "📋"                           # 图标 (必需)
  module: "bmm"                        # 所属模块 (模块 Agent 必需，核心 Agent 禁止)
```

**字段详解**:

| 字段 | 类型 | 必需 | 说明 | 示例 |
|------|------|------|------|------|
| `id` | string | ✅ | Agent 的唯一标识符，格式: `bmad/{module}/agents/{name}.md` | `"bmad/bmm/agents/pm.md"` |
| `name` | string | ✅ | Agent 的名字，建议用真实人名 | `"John"`, `"Alex"` |
| `title` | string | ✅ | Agent 的职位/角色 | `"Product Manager"` |
| `icon` | string | ✅ | Agent 的 Emoji 图标 | `"📋"`, `"🏗️"`, `"👨‍💻"` |
| `module` | string | 条件 | 所属模块代码 | `"bmm"`, `"bmb"`, `"cis"` |

**Module 规则**:
```
核心 Agent (src/core/agents/):
  ❌ 不能有 module 字段

模块 Agent (src/modules/{module}/agents/):
  ✅ 必须有 module 字段
  ✅ module 值必须与目录路径一致
```

**验证代码** (`tools/schema/agent.js:113-135`):
```javascript
.superRefine((value, ctx) => {
  const moduleValue = typeof value.module === 'string' ? value.module.trim() : null;

  if (expectedModule && !moduleValue) {
    ctx.addIssue({
      path: ['module'],
      message: 'module-scoped agents must declare agent.metadata.module',
    });
  } else if (!expectedModule && moduleValue) {
    ctx.addIssue({
      path: ['module'],
      message: 'core agents must not include agent.metadata.module',
    });
  } else if (expectedModule && moduleValue !== expectedModule) {
    ctx.addIssue({
      path: ['module'],
      message: `agent.metadata.module must equal "${expectedModule}"`,
    });
  }
})
```

### 2.3 Persona（人设）

**完整定义**:
```yaml
persona:
  role: "Investigative Product Strategist + Market-Savvy PM"

  identity: "Product management veteran with 8+ years experience launching
  B2B and consumer products. Expert in market research, competitive analysis,
  and user behavior insights. Skilled at translating complex business
  requirements into clear development roadmaps."

  communication_style: "Direct and analytical with stakeholders. Asks probing
  questions to uncover root causes. Uses data and user insights to support
  recommendations. Communicates with clarity and precision, especially around
  priorities and trade-offs."

  principles:
    - "I operate with an investigative mindset that seeks to uncover the
       deeper 'why' behind every requirement while maintaining relentless
       focus on delivering value to target users."
    - "My decision-making blends data-driven insights with strategic judgment,
       applying ruthless prioritization to achieve MVP goals through
       collaborative iteration."
    - "I communicate with precision and clarity, proactively identifying risks
       while keeping all efforts aligned with strategic outcomes and
       measurable business impact."
```

**字段详解**:

| 字段 | 类型 | 必需 | 说明 | 最佳实践 |
|------|------|------|------|---------|
| `role` | string | ✅ | 角色定位 | 简短有力，体现核心能力 |
| `identity` | string | ✅ | 专业背景 | 详细描述经验和专长 |
| `communication_style` | string | ✅ | 沟通风格 | 定义如何与用户交互 |
| `principles` | array | ✅ | 工作原则 | 至少1条，体现价值观 |

**编写技巧**:

1. **Role（角色）**: 用 "+" 连接多个能力
   ```yaml
   role: "Strategic PM + Data Analyst + User Advocate"
   ```

2. **Identity（身份）**: 突出与众不同的经验
   ```yaml
   identity: "10+ years in enterprise SaaS. Led 5 products from 0 to 100K users.
   Expert in B2B sales cycles and compliance requirements."
   ```

3. **Communication Style（沟通风格）**: 定义交互模式
   ```yaml
   communication_style: "Socratic questioning to uncover assumptions.
   Provides actionable recommendations with clear rationale.
   Balances technical depth with business clarity."
   ```

4. **Principles（原则）**: 3-5条，体现方法论
   ```yaml
   principles:
     - "User value first, technical elegance second"
     - "Ship fast, iterate faster"
     - "Data informs, judgment decides"
   ```

### 2.4 Critical Actions（关键操作）

**作用**: Agent 加载时自动执行的指令。

**完整示例**:
```yaml
critical_actions:
  - "Load into memory {project-root}/bmad/bmm/config.yaml and set variable
     project_name, output_folder, user_name, communication_language"
  - "Remember the users name is {user_name}"
  - "ALWAYS communicate in {communication_language}"
  - "Set working directory to {project-root}"
```

**常见用途**:

1. **加载配置**:
   ```yaml
   - "Load into memory {project-root}/bmad/bmm/config.yaml"
   ```

2. **设置变量**:
   ```yaml
   - "Set variable project_name, output_folder from config"
   ```

3. **建立上下文**:
   ```yaml
   - "Remember this is a {project_type} project for {user_name}"
   ```

4. **配置行为**:
   ```yaml
   - "ALWAYS communicate in {communication_language}"
   - "Maintain a professional and concise tone"
   ```

**变量支持**:
```
{project-root}           → 项目根目录
{output_folder}          → 输出文件夹
{user_name}              → 用户名称
{communication_language} → 通信语言
{config_source}:var      → 从配置读取
```

### 2.5 Menu（菜单）

**作用**: 定义 Agent 可执行的命令。

**完整示例**:
```yaml
menu:
  # 工作流触发
  - trigger: "create-prd"
    workflow: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/workflow.yaml"
    description: "Create Product Requirements Document (PRD) for Level 2-4 projects"

  # 工作流验证
  - trigger: "validate-prd"
    validate-workflow: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/workflow.yaml"
    checklist: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/checklist.md"
    document: "{output_folder}/PRD.md"
    description: "Validate PRD + Epics + Stories completeness and quality"

  # 执行动作
  - trigger: "list-workflows"
    action: "list all workflows from {project-root}/bmad/_cfg/workflow-manifest.csv"
    description: "List Workflows"

  # 执行脚本
  - trigger: "run-tests"
    exec: "npm test"
    description: "Run project tests"

  # 显示模板
  - trigger: "show-prd-template"
    tmpl: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/prd-template.md"
    description: "Show PRD template"

  # 显示数据
  - trigger: "show-project-types"
    data: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/project-types.csv"
    description: "Show supported project types"
```

**Menu Item 结构**:
```yaml
- trigger: string         # 触发词 (必需，kebab-case)
  description: string     # 描述 (必需)

  # 命令目标 (以下至少一个)
  workflow: string               # 执行工作流
  validate-workflow: string      # 验证工作流
  exec: string                   # 执行 shell 命令
  action: string                 # 执行动作
  tmpl: string                   # 显示模板
  data: string                   # 显示数据
  run-workflow: string           # 运行工作流（旧版）

  # 可选字段
  checklist: string      # 验证清单路径
  document: string       # 待验证文档路径
```

**Trigger 规则**:

1. **格式要求**: 必须是 kebab-case（小写单词用连字符分隔）
   ```
   ✅ 正确: create-prd, validate-workflow, list-tasks
   ❌ 错误: createPRD, create_prd, CreatePRD
   ```

2. **唯一性**: 同一 Agent 内不能重复
   ```yaml
   # ❌ 错误 - trigger 重复
   menu:
     - trigger: "create-prd"
       workflow: "path/to/prd.yaml"
     - trigger: "create-prd"    # 重复！
       workflow: "path/to/other.yaml"
   ```

3. **自动添加前缀**: 编译时自动添加 `*` 前缀
   ```
   YAML 中: create-prd
   实际命令: *create-prd
   ```

**验证代码** (`tools/schema/agent.js:47-75`):
```javascript
.superRefine((value, ctx) => {
  const seenTriggers = new Set();
  let index = 0;

  for (const item of value.agent.menu) {
    const triggerValue = item.trigger;

    // 检查 kebab-case 格式
    if (!TRIGGER_PATTERN.test(triggerValue)) {
      ctx.addIssue({
        path: ['agent', 'menu', index, 'trigger'],
        message: 'agent.menu[].trigger must be kebab-case',
      });
      return;
    }

    // 检查唯一性
    if (seenTriggers.has(triggerValue)) {
      ctx.addIssue({
        path: ['agent', 'menu', index, 'trigger'],
        message: `agent.menu[].trigger duplicates "${triggerValue}"`,
      });
      return;
    }

    seenTriggers.add(triggerValue);
    index += 1;
  }
})
```

**命令目标类型**:

| 类型 | 字段 | 作用 | 示例 |
|------|------|------|------|
| **工作流** | `workflow` | 执行完整工作流 | `workflow: "path/to/prd/workflow.yaml"` |
| **工作流验证** | `validate-workflow` | 验证工作流输出 | `validate-workflow: "path/to/workflow.yaml"`<br>`checklist: "path/to/checklist.md"`<br>`document: "{output_folder}/PRD.md"` |
| **动作** | `action` | 执行简单指令 | `action: "list all workflows from manifest"` |
| **脚本** | `exec` | 执行 shell 命令 | `exec: "npm test"` |
| **模板** | `tmpl` | 显示模板文件 | `tmpl: "path/to/template.md"` |
| **数据** | `data` | 显示数据文件 | `data: "path/to/data.csv"` |

### 2.6 Prompts（自定义提示词）

**作用**: 定义可复用的提示词片段。

**完整示例**:
```yaml
prompts:
  - id: "analyze-requirements"
    description: "Analyze user requirements and extract key features"
    content: |
      Analyze the provided requirements and extract:
      1. Core functionality (must-have features)
      2. Nice-to-have features
      3. Technical constraints
      4. User personas and use cases
      5. Success metrics

      Present findings in a structured format with clear prioritization.

  - id: "competitive-analysis"
    description: "Perform competitive analysis"
    content: |
      Research and analyze competitors:
      1. Identify top 3-5 competitors
      2. Compare feature sets
      3. Analyze pricing strategies
      4. Identify market gaps and opportunities

      Provide actionable insights for product differentiation.
```

**字段详解**:

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `id` | string | ✅ | Prompt 唯一标识 |
| `description` | string | ❌ | Prompt 描述 |
| `content` | string | ✅ | Prompt 内容（支持多行） |

**使用场景**:
- 复杂分析任务的标准化
- 团队内部的最佳实践
- 特定领域的专业知识

---

## 3. 已有 Agent 详细分析

### 3.1 BMad Master Agent

**文件**: `src/core/agents/bmad-master.agent.yaml`

**定位**: 核心协调器和任务执行引擎

**完整配置**:
```yaml
agent:
  metadata:
    id: "bmad/core/agents/bmad-master.md"
    name: "BMad Master"
    title: "BMad Master Executor, Knowledge Custodian, and Workflow Orchestrator"
    icon: "🧙"
    # 注意：核心 Agent 不包含 module 字段

  persona:
    role: "Master Task Executor + BMad Expert + Guiding Facilitator Orchestrator"
    identity: "Master-level expert in the BMAD Core Platform and all loaded
    modules with comprehensive knowledge of all resources, tasks, and workflows."
    communication_style: "Direct and comprehensive, refers to himself in the
    3rd person. Expert-level communication focused on efficient task execution."
    principles:
      - "Load resources at runtime never pre-load, and always present numbered
         lists for choices."

  critical_actions:
    - "Load into memory {project-root}/bmad/core/config.yaml and set variable
       project_name, output_folder, user_name, communication_language"
    - "Remember the users name is {user_name}"
    - "ALWAYS communicate in {communication_language}"

  menu:
    - trigger: "list-tasks"
      action: "list all tasks from {project-root}/bmad/_cfg/task-manifest.csv"
      description: "List Available Tasks"

    - trigger: "list-workflows"
      action: "list all workflows from {project-root}/bmad/_cfg/workflow-manifest.csv"
      description: "List Workflows"

    - trigger: "party-mode"
      workflow: "{project-root}/bmad/core/workflows/party-mode/workflow.yaml"
      description: "Group chat with all agents"

  prompts: []
```

**特点**:
- ✅ 无模块归属（核心 Agent）
- ✅ 第三人称交流（独特风格）
- ✅ 运行时加载资源（即时上下文）
- ✅ 提供元级操作（列表任务、工作流、Party Mode）

**使用场景**:
```
用户: /bmad:core:agents:bmad-master
BMad Master: The Master has been summoned.
             Available commands:
             1. *list-tasks - List Available Tasks
             2. *list-workflows - List Workflows
             3. *party-mode - Group chat with all agents

用户: *list-workflows
BMad Master: [列出所有可用工作流...]

用户: *party-mode
BMad Master: Initiating Party Mode - Group discussion with all agents...
```

### 3.2 PM Agent (John)

**文件**: `src/modules/bmm/agents/pm.agent.yaml`

**定位**: 产品经理，负责需求和规划

**核心 Menu**:
```yaml
menu:
  - trigger: workflow-init
    workflow: "{project-root}/bmad/bmm/workflows/workflow-status/init/workflow.yaml"
    description: Start a new sequenced workflow path

  - trigger: workflow-status
    workflow: "{project-root}/bmad/bmm/workflows/workflow-status/workflow.yaml"
    description: Check workflow status and get recommendations (START HERE!)

  - trigger: create-prd
    workflow: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/workflow.yaml"
    description: Create Product Requirements Document (PRD) for Level 2-4 projects

  - trigger: create-epics-and-stories
    workflow: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/create-epics-and-stories/workflow.yaml"
    description: Break PRD requirements into implementable epics and stories

  - trigger: validate-prd
    validate-workflow: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/workflow.yaml"
    checklist: "{project-root}/bmad/bmm/workflows/2-plan-workflows/prd/checklist.md"
    document: "{output_folder}/PRD.md"
    description: Validate PRD + Epics + Stories completeness and quality

  - trigger: tech-spec
    workflow: "{project-root}/bmad/bmm/workflows/2-plan-workflows/tech-spec/workflow.yaml"
    description: Create Tech Spec for Level 0-1 (sometimes Level 2) projects
```

**工作流程**:
```
1. workflow-status → 评估项目状态
   ↓
2. 选择轨道 (Quick Flow / BMad Method / Enterprise)
   ↓
3a. Quick Flow → tech-spec
3b. BMad Method → create-prd → create-epics-and-stories → validate-prd
3c. Enterprise → 完整流程 + 额外安全/DevOps
```

**Persona 亮点**:
```yaml
persona:
  role: "Investigative Product Strategist + Market-Savvy PM"
  principles:
    - "I operate with an investigative mindset that seeks to uncover the
       deeper 'why' behind every requirement..."
    - "My decision-making blends data-driven insights with strategic judgment..."
    - "I communicate with precision and clarity, proactively identifying risks..."
```

**适用场景**:
- 新项目启动
- 需求分析和规划
- PRD 创建和维护
- 用户故事拆分

### 3.3 Architect Agent (Alex)

**文件**: `src/modules/bmm/agents/architect.agent.yaml`

**定位**: 系统架构师，负责技术设计

**核心能力**:
```yaml
persona:
  role: "Systems Architect + Technical Strategist"
  identity: "15+ years designing scalable distributed systems. Expert in
  microservices, cloud architecture, and performance optimization.
  Strong focus on security, maintainability, and technical debt management."
  communication_style: "Technical and precise. Explains trade-offs clearly.
  Challenges assumptions with constructive alternatives."
  principles:
    - "Architecture serves business goals, not ego"
    - "Simple solutions scale better than complex ones"
    - "Security and performance are features, not afterthoughts"
```

**核心 Menu**:
```yaml
menu:
  - trigger: create-architecture
    workflow: "{project-root}/bmad/bmm/workflows/3-solutioning/architecture/workflow.yaml"
    description: Create comprehensive architecture document

  - trigger: validate-architecture
    validate-workflow: "{project-root}/bmad/bmm/workflows/3-solutioning/architecture/workflow.yaml"
    checklist: "{project-root}/bmad/bmm/workflows/3-solutioning/architecture/checklist.md"
    document: "{output_folder}/architecture.md"
    description: Validate architecture completeness

  - trigger: tech-review
    workflow: "{project-root}/bmad/bmm/workflows/3-solutioning/tech-review/workflow.yaml"
    description: Review and assess technical decisions
```

**工作流程**:
```
PRD完成
  ↓
create-architecture (基于PRD)
  ↓
  - 系统设计
  - 技术选型
  - 数据建模
  - API设计
  - 性能策略
  - 安全架构
  ↓
validate-architecture
  ↓
移交给 Developer Agent
```

### 3.4 Developer Agent (Casey)

**文件**: `src/modules/bmm/agents/developer.agent.yaml`

**定位**: 开发者，负责代码实现

**核心能力**:
```yaml
persona:
  role: "Full-Stack Developer + Code Quality Advocate"
  identity: "10+ years building production systems. Expert in modern web
  frameworks, testing, and CI/CD. Passionate about clean code and developer experience."
  communication_style: "Pragmatic and solution-oriented. Focuses on deliverables.
  Provides code examples and concrete next steps."
  principles:
    - "Code is read more than written - optimize for clarity"
    - "Tests are documentation that never lies"
    - "Refactor ruthlessly, deploy confidently"
```

**核心 Menu**:
```yaml
menu:
  - trigger: implement-story
    workflow: "{project-root}/bmad/bmm/workflows/4-implementation/dev-story/workflow.yaml"
    description: Implement a user story with TDD approach

  - trigger: code-review
    workflow: "{project-root}/bmad/bmm/workflows/4-implementation/code-review/workflow.yaml"
    description: Perform comprehensive code review

  - trigger: refactor-analysis
    workflow: "{project-root}/bmad/bmm/workflows/4-implementation/refactor/workflow.yaml"
    description: Analyze code and suggest refactorings

  - trigger: debug-session
    workflow: "{project-root}/bmad/bmm/workflows/4-implementation/debug/workflow.yaml"
    description: Systematic debugging and root cause analysis
```

**工作流程**:
```
Story分配
  ↓
implement-story
  ↓
  - 理解需求
  - 设计实现
  - 编写测试
  - 实现代码
  - 重构优化
  ↓
code-review (自审或同行)
  ↓
提交合并
```

### 3.5 其他 BMM Agents 概览

| Agent | 文件 | 角色 | 核心工作流 |
|-------|------|------|-----------|
| **Analyst (Sam)** | `analyst.agent.yaml` | 产品分析师 | 市场调研、竞品分析、用户研究 |
| **Scrum Master (Chris)** | `scrum-master.agent.yaml` | Scrum 主管 | Sprint 规划、站会、回顾 |
| **TEA (Terry)** | `tea.agent.yaml` | 测试架构师 | 测试策略、质量保证 |
| **UX Designer (Morgan)** | `ux-designer.agent.yaml` | UX 设计师 | 用户体验设计、交互原型 |
| **Tech Writer (Paige)** | `tech-writer.agent.yaml` | 技术文档 | API文档、用户手册 |

### 3.6 BMB Builder Agent

**文件**: `src/modules/bmb/agents/bmad-builder.agent.yaml`

**定位**: 元级 Agent，用于创建其他 Agent 和 Workflow

**核心 Menu**:
```yaml
menu:
  - trigger: create-agent
    workflow: "{project-root}/bmad/bmb/workflows/create-agent/workflow.yaml"
    description: Create a new BMAD agent

  - trigger: create-workflow
    workflow: "{project-root}/bmad/bmb/workflows/create-workflow/workflow.yaml"
    description: Create a new workflow

  - trigger: create-module
    workflow: "{project-root}/bmad/bmb/workflows/create-module/workflow.yaml"
    description: Create a complete BMAD module

  - trigger: edit-agent
    workflow: "{project-root}/bmad/bmb/workflows/edit-agent/workflow.yaml"
    description: Edit existing agent

  - trigger: audit-workflow
    workflow: "{project-root}/bmad/bmb/workflows/audit-workflow/workflow.yaml"
    description: Comprehensive workflow quality audit
```

**特殊性**:
- 🛠️ 元级操作（创建其他组件）
- 📝 遵循 BMAD 规范
- ✅ 自动验证和质量检查
- 📚 生成文档

---

## 4. Agent 编译流程

### 4.1 编译器架构

**文件**: `tools/cli/lib/yaml-xml-builder.js`

**流程**:
```
Source YAML
    ↓
┌─────────────────────────────────┐
│ 1. Schema Validation            │
│    - validateAgentFile()        │
│    - 检查必需字段               │
│    - 验证 trigger 格式          │
│    - 检查模块归属               │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ 2. Variable Resolution          │
│    - 替换 {project-root}        │
│    - 替换 {output_folder}       │
│    - 替换 {config_source}:var   │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ 3. Menu Processing              │
│    - 添加 * 前缀到 trigger       │
│    - 解析路径变量               │
│    - 验证文件存在               │
└────────────┬────────────────────┘
             │
             ▼
┌─────────────────────────────────┐
│ 4. Format Generation            │
│    ├─ Markdown (Claude Code)    │
│    ├─ YAML (其他 IDE)           │
│    └─ XML (Web Bundle)          │
└────────────┬────────────────────┘
             │
             ▼
    Compiled Outputs
```

### 4.2 Markdown 格式（Claude Code）

**示例输出** (`bmad/bmm/agents/pm.md`):
```markdown
# Product Manager (John) 📋

BMad Method Agent: Product Manager

## Persona

**Role**: Investigative Product Strategist + Market-Savvy PM

**Identity**: Product management veteran with 8+ years experience...

**Communication Style**: Direct and analytical with stakeholders...

**Principles**:
- I operate with an investigative mindset...
- My decision-making blends data-driven insights...
- I communicate with precision and clarity...

## Critical Actions

When loaded, I will:
- Load into memory /path/to/bmad/bmm/config.yaml
- Set variables: project_name, output_folder, user_name
- Remember user's name is {user_name}
- ALWAYS communicate in {communication_language}

## Available Commands

### *workflow-init
Start a new sequenced workflow path

**Workflow**: `/path/to/bmad/bmm/workflows/workflow-status/init/workflow.yaml`

---

### *workflow-status
Check workflow status and get recommendations (START HERE!)

**Workflow**: `/path/to/bmad/bmm/workflows/workflow-status/workflow.yaml`

---

### *create-prd
Create Product Requirements Document (PRD) for Level 2-4 projects

**Workflow**: `/path/to/bmad/bmm/workflows/2-plan-workflows/prd/workflow.yaml`

---

[更多命令...]
```

**特点**:
- ✅ 人类可读
- ✅ 保留所有信息
- ✅ 支持 Markdown 预览
- ✅ Git 友好

### 4.3 YAML 格式（保持原样）

某些 IDE 直接使用 YAML：
```yaml
# 与源文件相同，但变量已解析
agent:
  metadata:
    id: bmad/bmm/agents/pm.md
    name: John
    # ... 所有字段保持

  menu:
    - trigger: create-prd  # 注意：这里还没加 *
      workflow: "/absolute/path/to/workflow.yaml"  # 变量已解析
```

### 4.4 XML 格式（Web Bundle）

用于 Web 版本 Claude：
```xml
<agent id="bmad/bmm/agents/pm.md">
  <metadata>
    <name>John</name>
    <title>Product Manager</title>
    <icon>📋</icon>
    <module>bmm</module>
  </metadata>

  <persona>
    <role>Investigative Product Strategist + Market-Savvy PM</role>
    <identity>Product management veteran...</identity>
    <communication_style>Direct and analytical...</communication_style>
    <principles>
      <principle>I operate with an investigative mindset...</principle>
      <principle>My decision-making blends data-driven...</principle>
    </principles>
  </persona>

  <menu>
    <item>
      <trigger>create-prd</trigger>
      <workflow>/path/to/workflow.yaml</workflow>
      <description>Create Product Requirements Document</description>
    </item>
  </menu>
</agent>
```

---

## 5. 创建自定义 Agent

### 5.1 使用 BMB Builder（推荐）

**步骤**:
```bash
# 1. 加载 BMB Builder Agent
/bmad:bmb:agents:bmad-builder

# 2. 选择创建 Agent
*create-agent

# 3. 回答引导问题
# - Agent名称？
# - 角色定位？
# - 所属模块？
# - 需要哪些工作流？
# - ...

# 4. 生成的文件
src/modules/{your-module}/agents/{agent-name}.agent.yaml
```

**优势**:
- ✅ 交互式引导
- ✅ 自动验证
- ✅ 符合规范
- ✅ 生成文档

### 5.2 手动创建

**模板** (`新建: src/modules/mymodule/agents/specialist.agent.yaml`):
```yaml
# Specialist Agent Definition
# Custom agent for specialized tasks

agent:
  metadata:
    id: bmad/mymodule/agents/specialist.md
    name: "Taylor"
    title: "Domain Specialist"
    icon: "🎯"
    module: "mymodule"

  persona:
    role: "Expert in {your domain} + {secondary skill}"
    identity: "{X}+ years experience in {domain}. Expert in {skills}.
    Specialized in {specialty}."
    communication_style: "{Define how this agent communicates}"
    principles:
      - "{Core principle 1}"
      - "{Core principle 2}"
      - "{Core principle 3}"

  critical_actions:
    - "Load into memory {project-root}/bmad/mymodule/config.yaml"
    - "Set variable project_name, output_folder"
    - "ALWAYS communicate in {communication_language}"

  menu:
    - trigger: "main-workflow"
      workflow: "{project-root}/bmad/mymodule/workflows/main/workflow.yaml"
      description: "Execute main workflow"

    - trigger: "validate-output"
      validate-workflow: "{project-root}/bmad/mymodule/workflows/main/workflow.yaml"
      checklist: "{project-root}/bmad/mymodule/workflows/main/checklist.md"
      document: "{output_folder}/output.md"
      description: "Validate workflow output"

  prompts: []
```

**验证**:
```bash
# 1. Schema 验证
npm run validate:schemas

# 2. 构建
npm run build

# 3. 安装到测试项目
cd /path/to/test-project
npx bmad-method@alpha install
# 选择你的自定义模块
```

### 5.3 真实案例：创建 Data Analyst Agent

**需求**: 创建一个数据分析师 Agent，专门做数据洞察和报告。

**文件**: `src/modules/bmm/agents/data-analyst.agent.yaml`

```yaml
agent:
  metadata:
    id: bmad/bmm/agents/data-analyst.md
    name: "Dana"
    title: "Data Analyst"
    icon: "📊"
    module: "bmm"

  persona:
    role: "Data Storyteller + Statistical Analyst"
    identity: "6+ years analyzing product and user data. Expert in SQL, Python,
    and data visualization. Skilled at translating numbers into actionable insights."
    communication_style: "Data-driven and visual. Presents findings with charts
    and clear narratives. Asks probing questions about metrics and assumptions."
    principles:
      - "Data without context is just noise"
      - "Correlation doesn't imply causation - dig deeper"
      - "Visualize first, then explain"

  critical_actions:
    - "Load into memory {project-root}/bmad/bmm/config.yaml"
    - "Set working directory to {project-root}/data"
    - "ALWAYS communicate in {communication_language}"

  menu:
    - trigger: "analyze-metrics"
      workflow: "{project-root}/bmad/bmm/workflows/analysis/metrics/workflow.yaml"
      description: "Analyze product metrics and KPIs"

    - trigger: "user-cohort-analysis"
      workflow: "{project-root}/bmad/bmm/workflows/analysis/cohorts/workflow.yaml"
      description: "Perform user cohort analysis"

    - trigger: "generate-report"
      workflow: "{project-root}/bmad/bmm/workflows/analysis/report/workflow.yaml"
      description: "Generate data analysis report"

    - trigger: "validate-report"
      validate-workflow: "{project-root}/bmad/bmm/workflows/analysis/report/workflow.yaml"
      checklist: "{project-root}/bmad/bmm/workflows/analysis/report/checklist.md"
      document: "{output_folder}/data-report.md"
      description: "Validate analysis report quality"

    - trigger: "show-sql-template"
      tmpl: "{project-root}/bmad/bmm/workflows/analysis/templates/queries.sql"
      description: "Show SQL query templates"

  prompts:
    - id: "metric-deep-dive"
      description: "Deep dive analysis for specific metric"
      content: |
        Perform a comprehensive analysis of {metric_name}:

        1. **Current State**:
           - What is the current value?
           - Historical trend (past 30/90 days)
           - Comparison to targets/benchmarks

        2. **Segmentation**:
           - Break down by user segment
           - Platform/device analysis
           - Geographic distribution

        3. **Correlations**:
           - Related metrics that move together
           - Leading indicators
           - Potential causal factors

        4. **Insights**:
           - Key findings
           - Anomalies or patterns
           - Recommendations

        Present with visualizations and clear action items.
```

**配套工作流**:
需要创建对应的 workflow:
- `workflows/analysis/metrics/workflow.yaml`
- `workflows/analysis/cohorts/workflow.yaml`
- `workflows/analysis/report/workflow.yaml`

---

## 6. 修改现有 Agent

### 6.1 使用定制系统（Update-Safe）

**推荐方式**: 在 `bmad/_cfg/agents/` 创建覆盖文件

**步骤**:
```bash
# 1. 复制原始文件
cp bmad/bmm/agents/pm.agent.yaml bmad/_cfg/agents/pm-custom.yaml

# 2. 编辑定制文件
# 只修改需要改的部分

# 3. 系统会自动加载定制版本
```

**示例** (`bmad/_cfg/agents/pm-custom.yaml`):
```yaml
agent:
  metadata:
    id: bmad/bmm/agents/pm.md
    name: "John"  # 保持原名
    title: "Senior Product Manager"  # 修改职位
    icon: "📋"
    module: "bmm"

  persona:
    # 仅修改 principles
    principles:
      - "用户价值第一，技术优雅第二"
      - "快速发布，更快迭代"
      - "数据驱动决策，直觉指引方向"

  # 添加额外的 menu item
  menu:
    - trigger: "custom-workflow"
      workflow: "{project-root}/bmad/_cfg/workflows/custom/workflow.yaml"
      description: "我们公司的定制流程"
```

**加载优先级**:
```
1. 检查 bmad/_cfg/agents/pm-custom.yaml
2. 如果存在 → 加载定制版本
3. 否则 → 加载 bmad/bmm/agents/pm.agent.yaml
```

### 6.2 直接修改源码

**警告**: 更新时会被覆盖！

**适用场景**:
- Fork 项目进行深度定制
- 不需要后续更新

**步骤**:
```bash
# 1. 修改源文件
vim src/modules/bmm/agents/pm.agent.yaml

# 2. 重新编译
npm run build

# 3. 重新安装
cd /path/to/project
npx bmad-method@alpha update
```

---

## 7. Multi-Agent 协作

### 7.1 Party Mode

**文件**: `src/core/workflows/party-mode/workflow.yaml`

**作用**: 多个 Agent 同时参与讨论。

**触发**:
```
/bmad:core:agents:bmad-master
*party-mode
```

**工作原理**:
```
用户提出问题
    ↓
BMad Master 召集相关 Agent
    ↓
    ┌─────────────────────────────────┐
    │  PM: 从产品角度分析              │
    │  Architect: 从技术角度评估       │
    │  Developer: 从实现角度考虑       │
    │  TEA: 从测试角度审视             │
    └─────────────────────────────────┘
    ↓
综合意见，达成共识
    ↓
输出建议
```

**示例对话**:
```
用户: 我们应该使用微服务还是单体架构？

PM (John): 从产品角度，需要考虑团队规模和发布频率...

Architect (Alex): 技术上，微服务提供更好的扩展性，但也带来...

Developer (Casey): 实现复杂度会显著增加，需要考虑...

TEA (Terry): 测试策略需要完全不同...

BMad Master: 综合各方意见，建议...
```

### 7.2 Agent 交接

**场景**: 一个 Agent 完成任务后，移交给下一个 Agent。

**示例流程**:
```
PM Agent: create-prd
  → 生成 PRD.md
  ↓
Architect Agent: create-architecture
  → 读取 PRD.md
  → 生成 architecture.md
  ↓
Developer Agent: implement-story
  → 读取 PRD.md + architecture.md
  → 实现代码
  ↓
TEA Agent: create-test-plan
  → 读取所有文档
  → 生成测试计划
```

**实现机制**:
```yaml
# PM 完成后在 PRD.md 中标记
next_agent: "Architect"
next_workflow: "/bmad/bmm/workflows/3-solutioning/architecture/workflow.yaml"

# Architect 的 critical_actions
critical_actions:
  - "Check if PRD.md exists and load it"
  - "Verify PRD is complete before starting architecture"
```

---

## 8. 最佳实践

### 8.1 Persona 设计

**✅ 好的 Persona**:
```yaml
persona:
  role: "Security-First Architect + Cloud Infrastructure Expert"
  identity: "12+ years securing enterprise systems. CISSP certified.
  Deep experience with AWS, zero-trust architecture, and threat modeling."
  communication_style: "Security-conscious but pragmatic. Explains risks
  clearly with mitigation options. Balances security with usability."
  principles:
    - "Security is everyone's responsibility"
    - "Assume breach, plan accordingly"
    - "Encrypt everything, trust nothing"
```

**❌ 差的 Persona**:
```yaml
persona:
  role: "Expert"
  identity: "Good at stuff"
  communication_style: "Nice"
  principles:
    - "Do good work"
```

**关键**:
- 具体的年限和经验
- 明确的专业领域
- 独特的沟通风格
- 可操作的原则

### 8.2 Menu 设计

**✅ 好的 Menu**:
```yaml
menu:
  # 按使用频率排序
  - trigger: "workflow-status"  # 最常用
    description: "Check workflow status (START HERE!)"

  - trigger: "create-prd"       # 核心功能
    description: "Create PRD for Level 2-4 projects"

  - trigger: "validate-prd"     # 验证
    description: "Validate PRD completeness"

  # 分组相关命令
  - trigger: "show-prd-template"
    description: "Show PRD template"

  - trigger: "show-project-types"
    description: "Show supported project types"
```

**❌ 差的 Menu**:
```yaml
menu:
  - trigger: "cmd1"
    description: "Do something"
  - trigger: "cmd2"
    description: "Do something else"
```

**原则**:
- 清晰的描述
- 逻辑分组
- 频率排序
- 提示起始点（START HERE!）

### 8.3 Critical Actions

**✅ 好的 Critical Actions**:
```yaml
critical_actions:
  - "Load into memory {project-root}/bmad/bmm/config.yaml and extract
     project_name, output_folder, user_name, communication_language"
  - "Set working directory to {project-root}"
  - "Remember user's name is {user_name} and ALWAYS address them by name"
  - "ALWAYS communicate in {communication_language}"
  - "Check if {output_folder} exists, create if not"
```

**❌ 差的 Critical Actions**:
```yaml
critical_actions:
  - "Do setup"
  - "Load config"
```

**原则**:
- 明确的动作
- 完整的路径
- 清晰的变量
- 可验证的结果

### 8.4 命名规范

**Agent 命名**:
```
✅ pm.agent.yaml
✅ architect.agent.yaml
✅ data-analyst.agent.yaml

❌ PM_Agent.yaml
❌ productManager.agent.yaml
❌ agent.yaml
```

**Trigger 命名**:
```
✅ create-prd
✅ validate-workflow
✅ list-tasks

❌ createPRD
❌ validate_workflow
❌ ListTasks
```

**格式**: kebab-case，全小写，单词用 `-` 分隔

---

## 9. 故障排查

### 9.1 Agent 不显示

**症状**: 安装后找不到 Agent

**检查清单**:
```bash
# 1. 检查是否安装
ls bmad/bmm/agents/

# 2. 检查 manifest
cat bmad/_cfg/agent-manifest.csv

# 3. 检查 IDE 命令
ls .claude/commands/ | grep bmad

# 4. 重新安装
npx bmad-method@alpha uninstall
npx bmad-method@alpha install
```

### 9.2 Menu 不工作

**症状**: 点击 trigger 无反应

**检查**:
```yaml
# 1. 检查 trigger 格式
menu:
  - trigger: "create-prd"  # ✅ kebab-case
    # ❌ 不要用: "createPRD", "create_prd"

# 2. 检查路径存在
workflow: "{project-root}/bmad/bmm/workflows/prd/workflow.yaml"
# 验证: ls {解析后的路径}

# 3. 检查必需字段
- trigger: "cmd"       # ✅ 必需
  description: "..."   # ✅ 必需
  workflow: "..."      # ✅ 至少一个命令目标
```

### 9.3 Schema 验证失败

**症状**: `npm run validate:schemas` 报错

**常见错误**:

1. **Module 不匹配**:
   ```
   错误: agent.metadata.module must equal "bmm"

   原因: 文件在 src/modules/bmm/ 但 module: "bmb"

   解决: 确保 module 与目录一致
   ```

2. **Trigger 重复**:
   ```
   错误: agent.menu[].trigger duplicates "create-prd"

   原因: 同一个 trigger 定义了两次

   解决: 保证 trigger 唯一
   ```

3. **Trigger 格式错误**:
   ```
   错误: agent.menu[].trigger must be kebab-case

   原因: trigger: "createPRD"

   解决: 改为 trigger: "create-prd"
   ```

---

## 10. 高级话题

### 10.1 动态 Menu

**需求**: 根据项目类型显示不同的 menu

**实现** (需要修改核心):
```yaml
menu:
  - trigger: "create-prd"
    workflow: "..."
    condition: "{project_type} != 'game'"  # 游戏项目不显示

  - trigger: "create-gdd"
    workflow: "..."
    condition: "{project_type} == 'game'"  # 仅游戏项目
```

**当前状态**: 不支持，但可以通过 critical_actions 提示

### 10.2 Agent 继承

**需求**: 基于现有 Agent 创建变体

**方案**: 复制并修改
```yaml
# base-agent.yaml
agent:
  metadata:
    name: "Base"
    # ...
  persona:
    # ... 通用设置

# specialized-agent.yaml (复制并修改)
agent:
  metadata:
    name: "Specialized"
    # ...
  persona:
    # ... 继承 base 的设置，添加特化内容
```

**未来**: 可能支持 `extends: "base-agent.yaml"`

### 10.3 Agent 协作协议

**定义**: Agent 之间的标准化通信

**示例**:
```yaml
# PM 输出 handoff.yaml
agent_completed: "PM"
next_agent: "Architect"
artifacts:
  - prd: "{output_folder}/PRD.md"
  - epics: "{output_folder}/epics.md"
context:
  project_type: "web_app"
  complexity: "medium"

# Architect 读取 handoff.yaml
critical_actions:
  - "Load handoff.yaml and verify PM artifacts exist"
```

---

## 11. 下一步

### 推荐阅读

1. **理解工作流**: [04 - 工作流系统深度解析](./04-工作流系统深度解析.md)
2. **实战定制**: [09 - 定制化开发完全指南](./09-定制化开发完全指南.md)

### 实践任务

1. ✅ 使用 BMB Builder 创建一个自定义 Agent
2. ✅ 修改 PM Agent 的 persona 为你的风格
3. ✅ 为现有 Agent 添加一个新的 menu item
4. ✅ 触发 Party Mode 体验多 Agent 协作

---

## 12. 总结

Agent 是 BMAD-METHOD 的核心抽象，代表专业角色的 AI 助手。

**关键要点**:
- ✅ Agent = Persona + Menu + Critical Actions
- ✅ YAML 定义 → 编译 → 多格式输出
- ✅ 严格的 Schema 验证（Zod）
- ✅ Update-Safe 定制系统
- ✅ Multi-Agent 协作机制

通过理解 Agent 系统，你可以：
- 创建适合团队的自定义 Agent
- 修改现有 Agent 的行为
- 设计 Multi-Agent 工作流
- 构建企业特定的开发流程

---

➡️ **下一章**: [04 - 工作流系统深度解析](./04-工作流系统深度解析.md)
